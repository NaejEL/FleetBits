#!/usr/bin/env bash
# systemctl stub: records the call, never touches the host init system.
set -uo pipefail
echo "systemctl $*" >> "${STUB_LOG}"
case "${1:-}" in
  is-active) exit 1 ;;
  *) exit 0 ;;
esac
